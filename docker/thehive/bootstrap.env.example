# Copy to /root/thehive-bootstrap.env on soc-mgr-01 with mode 0600.
# Supply the current first-start administrator password out of band, then
# remove the bootstrap file after bootstrap_access.sh confirms rotation.
THEHIVE_INITIAL_ADMIN_PASSWORD=replace-at-deployment
